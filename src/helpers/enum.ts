export enum status {
  default = "default",
  open = "wcpt-scheduled",
  closed = "wcpt-closed",
}

export enum calendarView {
  year = "dayGridYear",
  month = "dayGridMonth",
  week = "timeGridWeek",
  day = "dayGridWeek",
  multi_month = "multiMonthYear",
}

export enum GeneralView {
  calendar = "calendar",
  map = "map",
}
