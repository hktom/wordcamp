export enum status {
  open = "wcpt-scheduled",
  closed = "wcpt-closed",
}

export enum calendarView {
  year = "dayGridYear",
  month = "dayGridMonth",
  week = "timeGridWeek",
  day = "dayGridWeek",
}
